import Ember from 'ember';

export default Ember.Controller.extend({
  forgot: [
    {footerLabel: 'Enter your email address to reset your password. You may need to check your spam folder or unblock no-reply@cform.io.'},
  ]
});
